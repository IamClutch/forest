<div id="review-list">
  <% @product.reviews.each do |review| %>
    <div class="review">
      <div class="poster">
         <%= review.user.name %> on <%= review.created_at %>
      </div>
      <p class="comment">
      <%= review.comment %>
      </p>
    </div>
  <% end %>
</div>
